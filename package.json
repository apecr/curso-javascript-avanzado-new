{
  "name": "curso-javascript-avanzado-new",
  "version": "1.1.0",
  "description": "Nuevos ejemplos",
  "main": "index.js",
  "scripts": {
    "deps": "npm install",
    "test": "./node_modules/istanbul/lib/cli.js cover ./node_modules/.bin/_mocha -- -u bdd --recursive",
    "coveralls": "cat ./coverage/lcov.info | ./node_modules/.bin/coveralls",
    "coverage": "node_modules/.bin/istanbul cover --report lcovonly --report html --dir coverage ./node_modules/.bin/_mocha -- -u bdd --recursive",
    "sonar": "sonar-scanner"
  },
  "author": "Alberto Eyo",
  "keywords": [
    "npm-library"
  ],
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.1.0",
    "istanbul": "^0.4.5",
    "mocha": "^7.1.2"
  }
}
